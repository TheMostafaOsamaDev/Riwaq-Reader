# Commits that touched a lot of lines without changing what any of them do.
# `git blame` should look straight through these to the commit that actually
# wrote the line.
#
# Turn it on once, per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically, so blame in the web UI is already
# correct without that step.
#
# Only add a commit here that is provably a no-op on behaviour — a formatter
# run, a bulk rename. Never a refactor, however mechanical it looked.

# style: format the codebase with Biome
83948f95c9553c6cc55ff282f9c3146881e5d923
